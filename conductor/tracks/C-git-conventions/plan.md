# Track C — Plan

## Tasks

1. Write `skills/git-conventions/SKILL.md`
2. Inspect frontmatter parses (eyeball or `python3 -c "import yaml; yaml.safe_load(open('skills/git-conventions/SKILL.md').read().split('---')[1])"` if pyyaml available)
