# Track G — README

## Goal

`README.md` covering what the toolkit is and how to use it.

## Sections

1. **Description** — one sentence
2. **Setup** — clone + suggested shell alias for `pull.sh`
3. **Usage** — all 8 commands with examples
4. **Available tools** — three tables (Skills, Hooks, Agents) with name + description
5. **Directory structure** — tree
6. **How to add a new tool** — minimal templates for skill, hook, agent
7. **What to commit in a target project** — guidance on `.claude/skills/*`, `.claude/agents/*`, `settings.json` edits
8. **Philosophy** — 4 bullets:
   - Pull only what you need
   - Copy not link
   - Hooks merge not replace
   - Re-pull is explicit

## Acceptance

- All 8 sections present
- All 8 commands documented with at least one example each
