# Track I — git init + commit

## Goal

Initialize git repo and create initial commit.

## Pre-conditions

- Track H passed
- User explicitly approves the commit

## Commands

```bash
git init
git add -A
git commit -m "chore: init arjun_claude_toolkit"
```

## Acceptance

- Repo has exactly one commit
- `git status` clean
