# Track A — Repo scaffold + .gitignore

## Goal

Establish empty directory structure and `.gitignore` so subsequent tracks have a place to write.

## Deliverables

- `skills/`, `hooks/`, `agents/` directories
- `.gitignore` with: `.DS_Store`, `__pycache__/`, `*.pyc`, `.python-version`

## Acceptance

- `ls` shows the three subdirs and `.gitignore`
- `.gitignore` content matches spec exactly
